name: Build and Deploy Code

on: [push, pull_request]

jobs:
  job1: 
    runs-on: ubuntu-latest
    steps:
      - name: pulling git repo
        uses: actions/checkout@v4 # to checkout the repo
      - name: say hi to Keroti
        run: echo "Hello Keroti"